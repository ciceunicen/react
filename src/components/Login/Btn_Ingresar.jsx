const Btn_Ingresar = ({ disabled }) => {
    // console.log(disabled)
    return (
        <button className="button" disabled={disabled}>
            INGRESAR
        </button>
    )
}

export default Btn_Ingresar